/* -- A listener to ensure the fonts we need to use have been loaded */
if (document.documentElement.className.indexOf('fonts-loaded') < 0) {
	// Change with self-hosted fontsLoaded
	
	// var FiraSansRegular = new FontFaceObserver('Fira Sans Regular', {
	// 	weight: 400,
	// });
	// var FiraSansMedium = new FontFaceObserver('Fira Sans Medium', {
	// 	weight: 500,
	// });
	// Promise.all([
	// 	FiraSansRegular.load(),
	// 	FiraSansMedium.load(),
	// ]).then(function() {
	// 	console.log('fonts-loaded');
	// 	document.documentElement.className += 'fonts-loaded';
	// 	Cookies.set('FONTS_LOADED', 1, { expires: 30 });
	// });
}
